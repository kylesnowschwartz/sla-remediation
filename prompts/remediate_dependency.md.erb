In the repository <%= repo %> there is an open issue #<%= issue.number %>
titled "<%= issue.title %>" (<%= issue.url %>). It was filed from pip-audit
output and reports that the pinned version of `<%= finding.package %>`
(<%= finding.pinned %>) in `requirements/base.txt` is affected by the
following advisories: <%= finding.advisories.join(", ") %>. The lowest
version that clears all of them is <%= finding.fix_version %>. Severity:
<%= finding.severity %>. Remediation is due by <%= due_at %> under
this repository's SECURITY-SLA.md policy.

Branch: `fix/<%= finding.package %>-sla-<%= issue.number %>`, off `master`.
<% if finding.major_version_bump? -%>
The fix crosses a major version (<%= finding.pinned %> → <%= finding.fix_version %>): the minimal source and test changes the upgrade breaks are permitted, and the structured output must include `breaking_changes` and `tests_run`.
<% else -%>
The fix stays within the same major version: change nothing but the `<%= finding.package %>` pin.
<% end -%>

Follow the attached playbook.
